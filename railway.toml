[build]
builder = "nixpacks"
buildCommand = "npm run install-all"

[deploy]
startCommand = "npm start"
# إما استخدم مسار بسيط للغاية أو قم بتعليق فحص الصحة مؤقتاً
# healthcheckPath = "/"
restartPolicyType = "on_failure"